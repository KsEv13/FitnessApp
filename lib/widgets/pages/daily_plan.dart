import 'package:flutter/material.dart';

// Здесть уже идет сама программа тренировок на день

class DailyPlan extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('FitnessApp'),
      ),
    );
  }
}
