import 'package:flutter/material.dart';

// Тренировки расписаны по дням в виде календаря на месяц.
// При выполнении дневного плана, отмечаются как завершенные

class TrainingCalendar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('FitnessApp'),
      ),
    );
  }
}
